<script type="speculationrules">
{
  "prerender": [
    {
      "source": "list",
      "urls": [
        {% for page in collections.all %}
          "{{ page.url }}"{% if not loop.last %},{% endif %}
        {% endfor %}
      ]
    }
  ]
}
</script>
